language: node_js
node_js:
- "6.9.4"
before_install:
  - pip install --user codecov
after_success:
  - codecov --file coverage/lcov.info --disable search
env:
  - CXX=g++-4.8
addons:
  sources:
    - ubuntu-toolchain-r-test
  apt:
    packages:
    - g++-4.8
